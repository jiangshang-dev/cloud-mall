# chatgpt-ui（商城）

用户端：商品上架浏览 → 下单（支付占位）→ 自动发放第 1 期卡密 → 订单详情可复制卡密并跳转激活站。

```bash
# 依赖可 symlink：ln -sfn ../../cloud-mall-views/node_modules node_modules
./node_modules/.bin/vite --host 127.0.0.1 --port 5174
```

打开 http://127.0.0.1:5174
